
def add_numbers(a, b):
    return a + b

# This will raise a TypeError because it tries to add a string and an integer
result = add_numbers("10", 5)  # TypeError: can only concatenate str (not "int") to str
