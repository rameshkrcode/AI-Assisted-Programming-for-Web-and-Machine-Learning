
# Input Code:
def calculate_average(scores):
    return sum(scores) / len(scores)

# AI Explanation:
# "The code assumes that the input list is non-empty. Adding a check for empty lists will prevent runtime errors."
